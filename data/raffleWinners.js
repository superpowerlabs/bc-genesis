// This is just an example. When it will be not, this message will be removed",

const allowList = require("../test/fixtures/winners");

// will be replaced by real winners when ready

module.exports = allowList;
